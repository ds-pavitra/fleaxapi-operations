# Toggle auth protection for the app
# When set to "true" the app will force unauthenticated users to /signin.
# If unset, the default is:
# - development mode: protection OFF (so devs can access pages while building)
# - production mode: protection ON
# To force protection in dev set:
VITE_AUTH_PROTECTION=true

# Session expiry fallback (in seconds)
# If your JWT does not include an "exp" claim, the app will use
# this value to compute session expiry (e.g., 900 = 15 minutes).
# Example: VITE_SESSION_EXPIRY_SECONDS=900
VITE_SESSION_EXPIRY_SECONDS=900

# API base URL used by the client (must be prefixed with VITE_)
# Example (local): VITE_API_BASE_URL=http://localhost:8080
# Example (prod):  VITE_API_BASE_URL=https://api.example.com
# VITE_API_BASE_URL=http://localhost:8080
